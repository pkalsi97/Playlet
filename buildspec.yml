version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing project dependencies..."
      - npm install --quiet
      
  pre_build:
    commands:
      - echo "Starting pre-build phase..."
      - echo "✓ Pre-build completed"
      
  build:
    commands:
      - echo "Starting build..."
      - rm -rf dist
      - npx tsc
      - echo "Running CloudFormation package..."
      - aws cloudformation package --template IaC/cloudformation/master.yml --s3-bucket $S3_BUCKET --output-template-file packaged.yml
      - echo "✓ Build completed"
      
  post_build:
    commands:
      - echo "Starting post-build cleanup..."
      - rm -rf node_modules
      - npm prune --omit=dev --quiet
      - echo "✓ Cleanup completed"      

artifacts:
  files:
    - packaged.yml
    - dist/**/*
    - package.json
    - package-lock.json
  base-directory: .